export enum UnidadMedidaEnum {
    libra = 3,
    arroba = 4,
    quintal =5,
    unidad = 6,
    docena = 7,
    caja = 8
}

export enum EquivalenciasMasaLibras {
    libra = 1,
    arroba = 25,
    quintal = 100
}

export enum EquivalenciasConteoUnidades {
    unidad = 1,
    docena = 12
}